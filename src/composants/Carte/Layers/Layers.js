import React from "react";

/**
 * Composant affichant une liste de layers pour la carte OpenLayer
 * @param children
 * @returns {JSX.Element}
 * @constructor
 */
const Layers = ({children}) => {
  return <div>{children}</div>;
};
export default Layers;