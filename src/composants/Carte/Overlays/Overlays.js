import React from "react";

/**
 * Composant affichant une liste d'overlays pour la carte OpenLayer
 * @param children
 * @returns {JSX.Element}
 * @constructor
 */
const Overlays = ({children}) => {
  return <div>{children}</div>;
};
export default Overlays;