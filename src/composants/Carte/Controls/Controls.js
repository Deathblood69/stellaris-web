import React from "react";

/**
 * Composant affichant une liste de contrôles pour la carte OpenLayer
 * @param children
 * @returns {JSX.Element}
 * @constructor
 */
const Controls = ({children}) => (
  <div>{children}</div>
)

export default Controls;